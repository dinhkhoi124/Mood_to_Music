cách chạy:

Frontend
cd Frontend/ 
-> npm i
-> npm run dev

FACE
cd Face
-> .\py310_env\Scripts\activate
-> py app1.py

------------------------------

17/11. cach chay
1. Kiểm tra thư mục môi trường ảo
Đảm bảo thư mục py310_env tồn tại tại đường dẫn:
Nếu thư mục không tồn tại hoặc bị thiếu file, bạn cần tạo lại môi trường ảo.
2. Tạo lại môi trường ảo
Nếu môi trường ảo bị hỏng, hãy xóa thư mục py310_env và tạo lại:

3. Kích hoạt môi trường ảo
Kích hoạt môi trường ảo mới tạo:

4. Cài đặt lại các thư viện
Sau khi kích hoạt môi trường ảo, cài đặt các thư viện từ file requirements.txt:

5. Cài đặt riêng librosa (nếu cần)
Nếu chỉ muốn cài đặt librosa, bạn có thể chạy:

6. Kiểm tra đường dẫn Python và Pip
Đảm bảo rằng python và pip đang sử dụng đúng môi trường ảo:

Kết quả phải trỏ đến:

Lưu ý:
Nếu bạn vẫn gặp lỗi, hãy kiểm tra phiên bản Python (python --version) và đảm bảo nó tương thích với các thư viện trong requirements.txt.
Nếu cần hỗ trợ thêm, hãy cung cấp thêm thông tin chi tiết!